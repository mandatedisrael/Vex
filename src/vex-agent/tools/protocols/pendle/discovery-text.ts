/**
 * Pendle discovery text — chain list for low-weight lexical recall.
 * Pendle is Ethereum-mainnet only in this wave (see @tools/pendle/constants).
 */

export const PENDLE_CHAINS: readonly string[] = ["ethereum"];
