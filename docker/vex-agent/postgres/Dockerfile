# Vex — Postgres + pgvector wrapper
# Owned by repo for explicit version pinning + room for init scripts.
# Pin: pgvector 0.8.2 + Postgres 18 on Debian Trixie.
# Bump only after reading both pgvector and Postgres release notes.
FROM pgvector/pgvector:0.8.2-pg18-trixie
# Future: COPY init/*.sql /docker-entrypoint-initdb.d/
